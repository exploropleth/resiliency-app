export const AppConfig = Object.freeze({
  dataPath: 'assets/data/',
  vegaLiteOptions: Object.freeze({
    renderer: "canvas",
    // actions: true, // POSTER
    actions: false,
    hover: true,
    tooltip: true
  })
});